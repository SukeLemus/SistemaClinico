@model SistemaClinico.Models.CirugiasPaciente
@using SistemaClinico.Controllers;
@using System.Data;
@using PagedList;
@using PagedList.Mvc;
@{
    ViewBag.Title = "AgregarC";
}

<div class="titulo_general"><h1>Cirugias</h1></div>



@using (Html.BeginForm())
{
    <div class="titulo_general">
        <h2>Agregando Cirugias al paciente: @ViewData["nombrep"] </h2>
    </div>

    <div class="form-horizontal">
        <hr />


        <div class="form-group">
            @Html.Label("Cirugias", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-9">
                @Html.DropDownList("ID_CIRUGIA", (IEnumerable<SelectListItem>)@ViewData["ListaCirugias"], "Seleccione una Cirugia", htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ID_CIRUGIA, "", new { @class = "text-danger" })
                @if (TempData["UserMessage"] != null)
                {
                    var message = TempData["UserMessage"];
                    <div class="text-danger">
                        @*<strong>@message</strong>*@
                        @message
                    </div>
                }
            </div>
        </div>



        <table align="center">
            <tr>
                <td>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Agregar Cirugias" class="btn btn-success" />
                        </div>
                    </div>
                </td>
                <td>&nbsp;  &nbsp; &nbsp;  &nbsp;</td>
                <td>
                    <div div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input onclick="location.href='@Url.Action("InformacionMedica", "Usuarios", new { id = @ViewData["idpaciente"]})'" type="button" value="Finalizar" class="btn btn-finalizar" />
                        </div>
                    </div>
                </td>
            </tr>
        </table>

    </div>
    <h2>Cirugias Agregadas:</h2>
    <div class="lista-sintoEnf">

        <div class="col-md-10">

            @Html.ListBox("IDs", (IEnumerable<SelectListItem>)@ViewData["ListaCirugiaID"], htmlAttributes: new { @class = "form-control" })


        </div>
    </div>







}

<div>
    @*@Html.ActionLink("Back to List", "Index")*@
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")


}
