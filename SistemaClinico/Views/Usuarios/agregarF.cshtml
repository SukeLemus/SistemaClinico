@model SistemaClinico.Models.FracturasPaciente
@using SistemaClinico.Controllers;
@using System.Data;
@using PagedList;
@using PagedList.Mvc;
@{
    ViewBag.Title = "AgregarF";
}

<div class="titulo_general"><h1>Fracturas</h1></div>



@using (Html.BeginForm())
{
    <div class="titulo_general">
        <h2>Agregando Fractura al paciente: @ViewData["nombrep"] </h2>
    </div>

    <div class="form-horizontal">
        <hr />


        <div class="form-group">
            @Html.Label("Fracturas", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-9">
                @Html.DropDownList("ID_FRACTURAS", (IEnumerable<SelectListItem>)@ViewData["ListaFracturas"], "Seleccione una Fractura", htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ID_FRACTURAS, "", new { @class = "text-danger" })
                @if (TempData["UserMessage"] != null)
                {
                    var message = TempData["UserMessage"];
                    <div class="text-danger">
                        @*<strong>@message</strong>*@
                        @message
                    </div>
                }
            </div>
        </div>



        <table align="center">
            <tr>
                <td>
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Agregar Fractura" class="btn btn-success" />
                        </div>
                    </div>
                </td>
                <td>&nbsp;  &nbsp; &nbsp;  &nbsp;</td>
                <td>
                    <div div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input onclick="location.href='@Url.Action("InformacionMedica", "Usuarios", new { id = @ViewData["idpaciente"]})'" type="button" value="Finalizar" class="btn btn-finalizar" />
                        </div>
                    </div>
                </td>
            </tr>
        </table>

    </div>
    <h2>Fracturas Agregadas:</h2>
    <div class="lista-sintoEnf">

        <div class="col-md-10">

            @Html.ListBox("IDs", (IEnumerable<SelectListItem>)@ViewData["ListaFracturasID"], htmlAttributes: new { @class = "form-control" })


        </div>
    </div>







}

<div>
    @*@Html.ActionLink("Back to List", "Index")*@
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")


}
